{
  "name": "followupgc-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "serve": "pnpm build && firebase emulators:start --only functions,firestore,storage",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^5.0.2",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
