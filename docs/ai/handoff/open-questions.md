# Open Questions

- Should the app eventually support export/import for local backups?
- When forms grow, which validation should be centralized with Zod first:
  members, sessions, or timeline entries?
- What threshold should trigger splitting `src/App.tsx` into feature folders?

These are not blockers for the current local-first app.
